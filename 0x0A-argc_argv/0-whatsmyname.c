#include <stdio.h>

/**
 * main - print program name followed by new line
 * @argc: int for argument counter
 * @argv: array of values
 * Return: 0
 */
int main(int __attribute__((unused)) argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
