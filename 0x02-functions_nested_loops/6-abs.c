#include "main.h"

/**
 * _abs - the function
 * Description: it recive number and return the absolute value
 * @n: is integer number which wer are checking
 * Return: Absoulte value of number
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
